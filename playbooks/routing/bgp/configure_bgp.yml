---
- name: Configure BGP routers idempotently
  hosts: ios_bgp_routers
  gather_facts: false

  tasks:
    - name: Subinterface encapsulation (no module coverage)
      cisco.ios.ios_config:
        lines:
          - "encapsulation dot1Q {{ item.encapsulation.vlan_id }}"
        parents: "interface {{ item.name }}"
      loop: "{{ l2_ifaces | default([])
                | selectattr('encapsulation', 'defined')
                | rejectattr('name', 'search', '^Loopback') | list }}"
      loop_control:
        label: "{{ item.name }}"
      register: encap_result

    - name: Interface addressing
      cisco.ios.ios_l3_interfaces:
        config: "{{ l3_ifaces }}"
        state: merged
      register: l3_result

    - name: Interface enablement and descriptions
      cisco.ios.ios_interfaces:
        config: "{{ interfaces }}"
        state: replaced
      register: intf_result

    - name: Show interface commands
      ansible.builtin.debug:
        msg:
          encap: "{{ encap_result.results | default([])
                     | selectattr('updates', 'defined')
                     | map(attribute='updates') | flatten }}"
          l3: "{{ l3_result.commands | default([]) }}"
          interfaces: "{{ intf_result.commands | default([]) }}"

    - name: Prefix lists
      cisco.ios.ios_prefix_lists:
        config: "{{ prefix_lists }}"
        state: replaced
      register: pl_result

    - name: Route maps
      cisco.ios.ios_route_maps:
        config: "{{ route_maps }}"
        state: replaced
      register: rm_result

    - name: BGP global
      cisco.ios.ios_bgp_global:
        config: "{{ bgp_global }}"
        state: replaced
      register: bgp_g_result

    - name: BGP address family
      cisco.ios.ios_bgp_address_family:
        config: "{{ bgp_af }}"
        state: replaced
      register: bgp_af_result

    - name: Show BGP commands
      ansible.builtin.debug:
        msg: "{{ (bgp_g_result.commands | default([])) + (bgp_af_result.commands | default([])) }}"
