---
- name: Deploy intended prod/default-VRF state
  hosts: ios
  gather_facts: False

  tasks:
    - name: Push intended prod config
      cisco.ios.ios_config:
        src: "../intended_state/{{ inventory_hostname }}.cfg"
        save_when: modified