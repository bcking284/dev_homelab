---
- name: Save running config to startup
  hosts: "{{ target | default('all') }}"
  gather_facts: false

  tasks:
    - name: Write memory
      cisco.ios.ios_config:
        save_when: always
      register: saved

    - name: Report
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} saved"
      when: saved is changed